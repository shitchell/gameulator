/target
# Trunk build artifacts for the wasm frontend
crates/web/dist/
/games/
# personal run notes / journey / trackers — never tracked
/personal/
**/*.gbc
**/*.sav
**/*.srm
/vendor/pokemon-yellow-legacy/*
